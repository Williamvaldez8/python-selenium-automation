from selenium import webdriver
from selenium.webdriver.common.by import By

# init driver
driver = webdriver.Chrome()
driver.maximize_window()

driver.find_element(by.id, id="twotabsearchtextbox")

drinver.find_element(by. path)